interface PlaybackControlsProps { readonly frame: number; readonly frameCount: number; readonly timeLabel: string; readonly isPlaying: boolean; readonly canPlay: boolean; readonly speed: number; readonly onPlay: () => void; readonly onPause: () => void; readonly onReset: () => void; readonly onStep: (amount: number) => void; readonly onFrame: (frame: number) => void; readonly onSpeed: (speed: number) => void }
export function PlaybackControls(props: PlaybackControlsProps) { const last = Math.max(0, props.frameCount - 1); return <div className="playback-controls"><div className="playback-controls__buttons"><button type="button" onClick={props.isPlaying ? props.onPause : props.onPlay} disabled={!props.canPlay} aria-pressed={props.isPlaying}>{props.isPlaying ? 'Pause' : 'Play'}</button><button type="button" onClick={() => props.onStep(-1)} disabled={props.frame === 0}>Step back</button><button type="button" onClick={() => props.onStep(1)} disabled={props.frame === last}>Step forward</button><button type="button" onClick={props.onReset} disabled={props.frame === 0}>Reset</button></div><label className="playback-controls__timeline">Progress <input type="range" min="0" max={last} value={props.frame} onChange={(event) => props.onFrame(Number(event.target.value))} aria-valuetext={props.timeLabel} /><output>{props.timeLabel}</output></label><label>Speed <select value={props.speed} onChange={(event) => props.onSpeed(Number(event.target.value))}><option value="0.5">0.5×</option><option value="1">1×</option><option value="2">2×</option><option value="4">4×</option></select></label></div> }
